services:
  affiliate-admin-v2:
    build:
      context: .
      dockerfile: Dockerfile
    image: affiliate-admin-v2:latest
    container_name: affiliate-admin-v2
    restart: always
    ports:
      - "5173:5173"
    env_file:
      - .env
